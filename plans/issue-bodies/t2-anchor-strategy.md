## Question

Given the Quartz block-reference findings: block ids (`[[resources#^<id>]]`) or heading-level (`[[resources#<tag>]]`)? Then settle:

- id derivation: hash algorithm + length, collision posture
- alias/display form of the rewritten link
- URL-change behavior: new URL means new id — who cleans up the orphaned old line?
