Resolved **Spec the verification agent** — design spec recorded on the ticket: manual agent sessions driven by a canonical `verify-resources` skill; URL-pattern classification (youtube/pdf/website) with YouTube grouped by video id and verified once; tiered fidelity — existence+topic always, claim-level on extractable text, transcript-level for YouTube via required yt-dlp (`unverifiable (tool missing)` otherwise); LLM judgment yields per-Resource verdicts `ok` / `dead` / `drift` / `unsupported-claim` / `misattribution` / `unverifiable`, each non-ok verdict quoting evidence from both sides; gitignored per-run report `.cache/verification/report-<date>.md` + chat summary, separate from lint's sync-time tier; registry, wrapper, and lint untouched. Full detail: https://github.com/qvd808/personal-knowledge-base/issues/41
