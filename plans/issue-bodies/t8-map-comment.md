Built **Build the change-review step** — `tools/change-review/` implemented to the #40 spec (`wordlist.ts`, `diff.ts`, `check.ts`, `review.ts` CLI, seeded `wordlist.txt`) with wrapper step `review` wired index→**review**→prompt; findings print lint-style and the count rides the halt message ("Sync complete. N finding(s)."); added-lines-only via `-U0` hunk parsing plus untracked notes; 6 module tests incl. a real-git end-to-end, 183/183 repo-wide; findings exit 0, infra failures exit 1. Full detail: https://github.com/qvd808/personal-knowledge-base/issues/44
