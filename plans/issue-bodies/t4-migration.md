## Question

Nothing to decide — manual work that unblocks the first real sync:

- Move FPGA prose resource-links (APIO repo, metastability experiment, FIFO) into that note's `## Resources` section; replace the FIFO YouTube-redirect URL with the real Cummings SNUG2002SJ FIFO1 PDF (`http://www.sunburst-design.com/papers/CummingsSNUG2002SJ_FIFO1.pdf`).
- Absorb the two hand-curated `resources.md` entries into the generated model.
- Add generated-section markers to `resources.md`; run the first rewrite pass; eyeball the diff together.
